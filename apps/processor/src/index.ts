import { RabbitMQConnection } from "./rabbitmq/rabbitmq-connection.js";

function main() {
    const rabbitmq_connection = new RabbitMQConnection();

    while(1) {
        const outbox_messages = new OutboxMessa
    }
}